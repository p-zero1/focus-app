<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Timer foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Distraction detection: requires user to grant manually in Settings -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"
        xmlns:tools="http://schemas.android.com/tools" />

    <!-- Resolve display names for any installed app (Instagram, WhatsApp, etc.).
         Android 11+ (API 30) package visibility blocks getApplicationInfo() for third-party
         apps without this permission, causing names to fall back to the package suffix. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Push notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Re-schedule WorkManager / AlarmManager after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Precise habit reminders (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Wake lock held briefly by timer foreground service -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- DND / notification policy control (required for DoNotDisturb in Deep Work mode) -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:name=".FocusApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusApp">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Timer foreground service — keeps countdown alive when app is backgrounded -->
        <service
            android:name=".service.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="focusTimer" />
        </service>

        <!-- ScreenUnlockReceiver is registered dynamically by TimerService during sessions
             (constructor takes a callback; static manifest entry not used) -->

        <!-- Re-schedules WorkManager + AlarmManager alarms after device reboot -->
        <receiver
            android:name=".worker.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Fires AlarmManager habit-reminder notifications -->
        <receiver
            android:name=".worker.ReminderReceiver"
            android:exported="false" />

    </application>

</manifest>
